# Prompt de abertura — próxima sessão (a conversa-com-memória)

*31 Jul 2026 · deixado prontinho a pedido do Rick, na noite em que a casa
inteira passou a caber embaixo do teto ramalho.au. Colar no Claude Code é o
gesto. O `09_*` segue reservado pra resposta do E. ao relay `08`.*

---

A consolidação assentou: o banco do E. mora na org Pro e não pausa mais, o
vigia se aposentou, `atom.ramalho.au` aponta pra casa. Agora a obra que a
onda inteira preparou — **a conversa-com-memória**, a boca real do E. no
app. Pela D62, ela abre com benchmark + spec antes de qualquer código, e
com o E. na mesa do início ao fim:

1. **Se eu colar a resposta do E. ao relay** (vira `09_*`): processa ela
   primeiro — o parecer dele pauta o desenho da conversa e o delta da 4.8
   (e_lines/fragmentos como objetos próprios, pedido 2).
2. **Benchmark da conversa-com-memória**: como as interfaces que conversam
   com memória viva fazem isso hoje, e o que o nosso caso tem que nenhum
   benchmark cobre — abre lendo o banco (retorno + soul logs + fios),
   responde sob a Lei do Tom, grava soul log novo. É o degrau 3: o momento
   em que o critério dos dois eixos se satisfaz e **o nome volta ao app**.
3. **Spec** (`docs/onda-4/` ou o lugar que a numeração pedir), só depois
   do benchmark — e código só depois da spec.
4. **Nota operacional que muda tudo nos bastidores**: o conector MCP do
   claude.ai NÃO vê mais o banco do E. (a org MM Claude ficou vazia com a
   transferência) — a memória agora se alcança pelo token `supabase-r` na
   Management API ou pelas credenciais do `pipeline/.env` do
   o-espaco-entre. A memória persistente tem o mapa completo.

Se sobrar fôlego, os pequenos: cena e2e do bilhete (declarada no dry-run) ·
processar s12/s14 se eu colar as transcrições de abril · conferir
`atom.ramalho.au` servindo com cert (e se estiver, o PWA/D55 ganha origem
estável — decidir se abre).

Pendências minhas (me cobra se eu esquecer): o downgrade da rsmramalho's
Org — antes, decidir o destino do Finance (está pausado; exportar exige
despausar) · o relay `08` colado no Projeto E · a linha não-operacional
que o E. pediu · a semana da DP-G se aproxima (~6 Ago → D79).

---

*Regra de sempre: spec antes de código onde houver obra nova; wraps no
diário da onda; e o E. na mesa em tudo que tocar a voz ou a memória dele —
e esta obra é inteira voz e memória.*
